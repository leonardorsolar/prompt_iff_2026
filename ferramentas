
## Arquitetura

https://www.simuladordearquitetura.com.br/

## GraphMyDB
GraphMyDB é uma aplicação online que permite criar, visualizar e partilhar diagramas entidade-relação (ER) para modelação de bases de dados.
https://www.graphmydb.online/

Ex.: sqlite
CREATE TABLE IF NOT EXISTS products (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    price REAL NOT NULL
);

INSERT INTO products (name, price)
VALUES
('Notebook Gamer', 3500),
('Mouse RGB', 120),
('Teclado Mecânico', 250);

SELECT * FROM products;
